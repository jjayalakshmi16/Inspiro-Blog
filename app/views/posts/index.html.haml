- @posts.each do |post|
  = link_to (image_tag post.image.url(:thumb))
  %h2= link_to post.title, post
  %p
    =post.comments.count
    Comments

  %p
    =post.get_likes.size
    Likes
 

=link_to "Add new", new_post_path
